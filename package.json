{
  "name": "lyppe-store",
  "version": "1.0.0",
  "private": true,
  "description": "Lyppe Store — digital product storefront (static HTML/CSS/Vanilla JS + Supabase). server.js is a local dev static file server only.",
  "scripts": {
    "dev": "node server.js"
  }
}
